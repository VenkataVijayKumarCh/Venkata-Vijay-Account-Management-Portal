@using Microsoft.AspNetCore.Identity
@using System.Security.Claims
@* @inject SignInManager<IdentityUser> SignInManager *@
@* @inject UserManager<IdentityUser> UserManager *@

@* Hello @User.Identity?.Name! *@

@* <ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello User!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>  *@

<section>

    @* @{
        string username = HttpContextAccessor.HttpContext?.Session.GetString("FirstName") 
        + ", " + HttpContextAccessor.HttpContext?.Session.GetString("LastName");
        int? userId = HttpContextAccessor.HttpContext?.Session.GetInt32("UserId");
    } *@

    @if(User.Identity!.IsAuthenticated)
        {
            @if(User.Identity != null & @User.Identity!.IsAuthenticated)
            {
                var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
                var userName = User.FindFirst(ClaimTypes.Name)?.Value;
                var email = User.FindFirst(ClaimTypes.Email)?.Value;
                string? loginUserName = User.FindFirst(ClaimTypes.GivenName)?.ToString().Split(": ")[1] + ", " + User.FindFirst(ClaimTypes.Surname)?.ToString().Split(": ")[1];
                // var userIdValue = User.FindFirst("UserId")?.Value;
                <p>Welcome, @loginUserName!</p>
                <p>Your User ID is: @userName</p>
                @* <p>Your User ID is: @userIdValue</p> *@
                <p></p>
            }
        }
</section>